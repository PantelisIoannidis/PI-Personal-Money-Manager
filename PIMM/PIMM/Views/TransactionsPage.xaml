<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:dg="clr-namespace:Xamarin.Forms.DataGrid;assembly=Xamarin.Forms.DataGrid"
             xmlns:conv="clr-namespace:PIMM.Views.Converters;assembly=PIMM"
             xmlns:fa="clr-namespace:PIMM.Helpers"
             mc:Ignorable="d"
             x:Class="PIMM.TransactionsPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="plus.png" 
                     Text="New Transaction"
                     Command="{Binding NewTransactionCommand}"
                     Order="Primary"/>
        <ToolbarItem Icon="plus.png" 
                     Text="Money Transfer"
                     Command="{Binding MoneyTransferCommand}"
                     Order="Secondary"/>
        <ToolbarItem Icon="plus.png" 
                     Text="Adjustment"
                     Command="{Binding AdjustmentCommand}" 
                     Order="Secondary"/>
    </ContentPage.ToolbarItems>
    <ContentView x:Name="PortraitView" Padding="20">

        <dg:DataGrid x:Name="transactionDatagrid"
                     ItemsSource="{Binding Transactions}" 
                     SelectionEnabled="True" 
                     SelectedItem="{Binding SelectedTransaction,Mode=TwoWay}"
                     RowHeight="70" HeaderHeight="50" BorderColor="Transparent" HeaderBackground="#E0E6F8"
				    PullToRefreshCommand="{Binding RefreshCommand}" 
                     IsRefreshing="{Binding IsRefreshing}"
					ActiveRowColor="#8899AA" 
                     ItemSelected="DataGrid_ItemSelected">
            <x:Arguments>
                <ListViewCachingStrategy>RetainElement</ListViewCachingStrategy>
            </x:Arguments>
            <dg:DataGrid.HeaderFontSize>
                <OnIdiom  x:TypeArguments="x:Double">
                    <OnIdiom.Tablet>15</OnIdiom.Tablet>
                    <OnIdiom.Phone>12</OnIdiom.Phone>
                </OnIdiom>
            </dg:DataGrid.HeaderFontSize>
            <dg:DataGrid.Columns>
                <dg:DataGridColumn Title="" PropertyName="Icon" SortingEnabled="False" Width="1*">
                    <dg:DataGridColumn.CellTemplate>
                        <DataTemplate>
                            <ContentView HorizontalOptions="Fill" VerticalOptions="Fill">
                                <Label Text="{Binding Name}" HorizontalOptions="Center" VerticalOptions="Center"
                                       FontFamily="{Binding FontFamily}"
                                       FontSize="Large"
                                       TextColor="{Binding Color}"/>
                            </ContentView>
                        </DataTemplate>
                    </dg:DataGridColumn.CellTemplate>
                </dg:DataGridColumn>
                <dg:DataGridColumn Title="Category" PropertyName="CategoryDescription"  Width="3*" />
                <dg:DataGridColumn Title="Subcategory" PropertyName="SubcategoryDescription"  Width="3*"/>
                <dg:DataGridColumn Title="Description" PropertyName="Description"  Width="4*"/>
                <dg:DataGridColumn Title="Date" PropertyName="TransactionDate"  Width="3*"/>
                <dg:DataGridColumn Title="Amount" PropertyName="Amount" Width="2*">
                    <dg:DataGridColumn.CellTemplate>
                        <DataTemplate>
                            <ContentView HorizontalOptions="Fill" VerticalOptions="Fill" 
						        BackgroundColor="{Binding Converter={StaticResource AmountToColorConverter}}">
                                <Label Text="{Binding}" HorizontalOptions="Center" VerticalOptions="Center" TextColor="Black"/>
                            </ContentView>
                        </DataTemplate>
                    </dg:DataGridColumn.CellTemplate>
                </dg:DataGridColumn>
            </dg:DataGrid.Columns>
            <dg:DataGrid.RowsBackgroundColorPalette>
                <dg:PaletteCollection>
                    <Color>#F2F2F2</Color>
                    <Color>#FFFFFF</Color>
                </dg:PaletteCollection>
            </dg:DataGrid.RowsBackgroundColorPalette>
            <dg:DataGrid.Resources>
                <ResourceDictionary>
                    <conv:AmountToColorConverter x:Key="AmountToColorConverter"/>
                </ResourceDictionary>
            </dg:DataGrid.Resources>
        </dg:DataGrid>
    </ContentView>
</ContentPage>